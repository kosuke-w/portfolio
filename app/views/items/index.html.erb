<hi>MY ITEMS</hi>

<% @items.each do |item| %>
<div>
  <%= link_to item_path(item.id) do %>
    <%= attachment_image_tag item, :image, :fill, 100, 100, fallback: 'noImage.jpg', size:'300x300' %>
  <% end %>
  <%= item.name %>
  <%= item.genre %>
</div>
<% end %>